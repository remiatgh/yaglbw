# yaglbw

This is the Yet Another Godot Launcher But Worse, there's a lot of Godot launchers, why not one more?

Made with C# on .NET 10

To build, well... don't know exactly but I think you can just run `dotnet pack` on the CLI folder to build the CLI NuGet package.